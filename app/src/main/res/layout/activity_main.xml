<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".BluetoothConnect"
    android:id="@+id/fragment_container">

    <!-- 'Hello World' and fragment testing button to be deleted.

    <TextView android:text="@string/hello_world"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/hello_world_msg"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Testing Fragment (Scott)"
        android:id="@+id/test_fragment_button"
        android:layout_below="@id/hello_world_msg"/>

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/fragment_container"
        android:layout_below="@id/test_fragment_button"/>
    -->

</FrameLayout>
